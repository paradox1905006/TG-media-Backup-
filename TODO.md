# Paradoxical Gallery — Phase 1 Implementation

## Status: In Progress

### Steps
- [x] A1. Add `@Inject` to `MediaStoreDataSource`
- [x] A2. Add `@Inject` to `GalleryRepository`
- [x] A3. Add `@HiltViewModel` + `@Inject` to `TimelineViewModel`
- [x] A4. Add `@HiltViewModel` + `@Inject` to `AlbumsViewModel`
- [x] B1. Create `di/AppModule.kt` (Room DB, DAOs)
- [x] C1. Create `gallery/ui/screens/TimelineScreen.kt` + `GalleryGridViewModel`
- [x] C2. Create `gallery/ui/screens/AlbumsScreen.kt`
- [x] C3. Create `gallery/ui/screens/AlbumDetailScreen.kt`
- [x] D1. Wire Gallery tab + routes into `AppNavigation.kt`
- [x] E1. Build attempted — blocked: no JDK/SDK on PATH in this shell. Build via Android Studio (`File → Sync` then `Build → Build APK`).

